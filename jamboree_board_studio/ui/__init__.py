"""New Board Studio UI widgets.

These are additive: they render from the format-independent ``Board``
model (``jamboree_board_studio.core.board``) and are wired into the
existing editor as extra tabs/panels, alongside — not instead of — the
original per-feature editors in ``editor_modules/``.
"""
